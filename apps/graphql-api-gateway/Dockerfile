# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build graphql-api-gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t graphql-api-gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system graphql-api-gateway && \
          adduser --system -G graphql-api-gateway graphql-api-gateway

COPY dist/apps/graphql-api-gateway graphql-api-gateway
RUN chown -R graphql-api-gateway:graphql-api-gateway .

CMD [ "node", "graphql-api-gateway" ]
