# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build profile-image-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t profile-image-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system profile-image-service && \
          adduser --system -G profile-image-service profile-image-service

COPY dist/apps/profile-image-service profile-image-service
RUN chown -R profile-image-service:profile-image-service .

CMD [ "node", "profile-image-service" ]
