# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build message-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t message-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system message-service && \
          adduser --system -G message-service message-service

COPY dist/apps/message-service message-service
RUN chown -R message-service:message-service .

CMD [ "node", "message-service" ]
