# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build message-subscription-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t message-subscription-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system message-subscription-service && \
          adduser --system -G message-subscription-service message-subscription-service

COPY dist/apps/message-subscription-service message-subscription-service
RUN chown -R message-subscription-service:message-subscription-service .

CMD [ "node", "message-subscription-service" ]
