/* import { graphqlMessageService } from './graphql-message-service';

describe('graphqlMessageService', () => {
  it('should work', () => {
    expect(graphqlMessageService()).toEqual('graphql-message-service');
  });
});
 */
