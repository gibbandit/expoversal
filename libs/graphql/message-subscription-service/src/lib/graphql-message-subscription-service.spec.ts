/* import { graphqlMessageSubscriptionService } from './graphql-message-subscription-service';

describe('graphqlMessageSubscriptionService', () => {
  it('should work', () => {
    expect(graphqlMessageSubscriptionService()).toEqual(
      'graphql-message-subscription-service'
    );
  });
});
 */
